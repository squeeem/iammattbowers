# Payload CMS
PAYLOAD_SECRET=generate_a_32_byte_hex_string
# Supabase Postgres connection string (Session/transaction pooler URI)
DATABASE_URI=postgresql://user:password@host:5432/postgres
# Vercel Blob storage (read/write token from the Vercel dashboard)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
